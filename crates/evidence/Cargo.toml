[package]
name = "firkin-evidence"
description = "Evidence catalogs, scorecards, and report validation for the firkin Rust containerization library"
version = "0.0.2"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
name = "firkin_evidence"
path = "src/lib.rs"

[dependencies]
firkin-trace = { path = "../trace", version = "0.0.2" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true
