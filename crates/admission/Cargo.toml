[package]
name = "firkin-admission"
description = "Admission control and capacity policy primitives for the firkin Rust containerization library"
version = "0.0.2"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
name = "firkin_admission"
path = "src/lib.rs"

[dependencies]
firkin-types = { path = "../types", version = "0.0.2" }
thiserror.workspace = true
firkin-artifacts = { path = "../artifacts", version = "0.0.2" }
