[package]
name = "firkin-hygiene"
description = "Runtime artifact cleanup and hygiene utilities for the firkin Rust containerization library"
version = "0.0.2"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
name = "firkin_hygiene"
path = "src/lib.rs"

[dependencies]
flate2.workspace = true
thiserror.workspace = true
firkin-artifacts = { path = "../artifacts", version = "0.0.2" }
